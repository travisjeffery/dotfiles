# vim:set ft=sh sw=2 sts=2:

cd ~/.vim


#for bundle in $(git submodule | cut -d' ' -f3); do
    #cd ~/.vim/$bundle && git pull
#done

for bundle in $(cat ~/bin/vimbundles); do
  if [ -d ~/.vim/$bundle ]; then
    cd ~/.vim/$bundle && git pull --rebase
    #TODO: clone plugin in they haven't been
  fi
done

#cd ~/.vim

#git submodule update
